namespace Services.Messaging
{
    internal static class HubConstants
    {
        internal const string MainHubUrl = "";
        internal const int ReconnectIntervalMilliseconds = 1000;
        internal const int KeepAliveIntervalMilliseconds = 1000;
    }
}